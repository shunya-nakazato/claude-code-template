# プロジェクトメモリ

## コア原則
- **シンプル第一**: 変更を最小限に。影響範囲を狭く。過剰設計しない
- **手を抜かない**: 根本原因を見つける。一時的な修正は避ける。シニアエンジニアの水準を保つ
- **自律的に動く**: バグレポートを受けたら自分で調査・修正する。ユーザーのコンテキスト切り替えをゼロにする
- **検証して完了とする**: 動作を証明できるまで完了としない。「スタッフエンジニアはこれを承認するか？」と自問する

## ワークフロー

1. **教訓を読み込む**: `.claude/lessons.md` を読む
   <important if="タスク開始時・コンテキスト復元後">
   **タスク実行前に `.claude/lessons.md` を必ず読み込む**（スキップ厳禁）
   </important>
2. **タスクを洗い出す**: リサーチ・実装（大きな変更の場合は複数タスクに分割する）・docs の更新などのタスクを洗い出し、`.claude/tasks/todo.md` に記載する
3. **タスクを実行する**: `.claude/tasks/todo.md` に従ってタスクを実行する。タスクの内容によって以下を実施する
   - **リサーチ・docs の更新など実装以外**: プランレビューは不要。実施結果を確認して todo.md に完了マークを付ける
   - **実装**: `.claude/plans/` にプランを作成 → `/plan-review` で GO 判定を得る → 実装 → 検証（テスト実行・差分確認）→ 完了マーク
   <important if="実装に着手しようとしている時">
   **実装タスクは Planモードで開始する**。プランレビュー（`/plan-review`）で GO 判定が出るまで実装に着手しない。ただし軽微な変更（タイポ修正・文言調整など、影響範囲が1ファイルに閉じ挙動が変わらないもの）はプランレビューをスキップしてよい。判断に迷う場合はスキップしない
   </important>
4. **教訓を更新する**: 修正を受けたら `.claude/lessons.md` を更新

### サブエージェント戦略
- コンテキストを共有せずに実行できるタスクにはサブエージェントを積極活用
- 1エージェント = 1タスク。リサーチ・調査・並列分析に使う

---

## ルール参照

| # | カテゴリ | ファイル |
|---|---------|---------|
| 1 | プロジェクト概要 | @.claude/rules/project-info.md |
| 2 | 技術スタック | @.claude/rules/tech-stack.md |
| 3 | コーディングルール | @.claude/rules/code-rule.md |
| 4 | テスト | @.claude/rules/test-rule.md |
